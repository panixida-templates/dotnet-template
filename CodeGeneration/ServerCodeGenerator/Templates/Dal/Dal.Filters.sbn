{{- $class_name = plural(model_name) + "Filters" -}}
{{- $search_params = plural(model_name) + "SearchParams" -}}

using Common.SearchParams;

using Dal.DbModels;

namespace Dal.Implementations.Filters;

internal static class {{ $class_name }}
{
    internal static IQueryable<{{ model_name }}> Filter(this IQueryable<{{ model_name }}> dbObjects, {{ $search_params }} searchParams)
    {
    {{~ if filters != "" ~}}
        {{ filters }}

    {{~ end ~}}
        return dbObjects;
    }
}