using Dal.DbModels;

using Microsoft.EntityFrameworkCore;

namespace Dal.Ef;

public class DefaultDbContext(DbContextOptions<DefaultDbContext> options) : DbContext(options)
{
    public virtual DbSet<{{ model_name }}> {{ plural(model_name) }} { get; set; }
}
